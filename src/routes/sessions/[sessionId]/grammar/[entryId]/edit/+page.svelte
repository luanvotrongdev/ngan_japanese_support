<script lang="ts">import GrammarForm from '$lib/components/GrammarForm.svelte'; let { data, form } = $props(); function confirmDelete(e: SubmitEvent) { if (!confirm('Delete this grammar entry?')) e.preventDefault(); }</script>
<div class="page-shell form-page"><a class="back" href={`/sessions/${data.session.id}`}>Back to {data.session.title}</a><section class="panel"><p class="eyebrow">Edit pattern</p><h1>{data.entry.grammar}</h1><form method="POST" action="?/update"><GrammarForm values={form?.values ?? data.entry} errors={form?.errors} submitLabel="Save grammar" /></form><form method="POST" action="?/delete" onsubmit={confirmDelete}><input type="hidden" name="confirmation" value="delete" /><button class="danger" type="submit">Delete grammar</button></form></section></div>
